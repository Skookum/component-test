extends ../../shared/views/layout

block title
  | Sign up - Components test

block content
  form.form-gateway(action='/signup', method='post')
    .title-bar
      h1 New account
    input.input-text(type='text', name='email', autocomplete='off')
    input.input-text(type='password', name='password', autocomplete='off')
    .button-bar
      button.button-submit(type='submit') Create